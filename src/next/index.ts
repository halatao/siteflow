export type SiteflowNextHelpersStatus = {
  available: false;
  reason: string;
};

export const siteflowNextHelpers: SiteflowNextHelpersStatus = {
  available: false,
  reason: "Next.js helper functions are reserved for a later implementation slice.",
};
